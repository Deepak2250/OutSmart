
server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/tripbuddy
    username: root
    password: djdanix2255
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        logging:
    level:
      org.hibernate.SQL: DEBUG
      org.hibernate.type.descriptor.sql.BasicBinder: TRACE
